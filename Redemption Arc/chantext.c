#include<stdio.h>
#include<string.h>

int main(){
    int *pointer;
    int num[10];
    char charac[10];

    pointer = num;

    // memset(charac,'g',5);
    // memset(charac+5,'b',5);

    // //printing arr
    // for(int i = 0 ; i < 10 ; i++){
    //     printf("%c", charac[i]);
    // }
}