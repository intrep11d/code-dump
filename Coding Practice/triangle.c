#include<stdio.h>

int main(){
    int a,b,c;
    printf("Enter angles: ");
    scanf("%d" "%d" "%d",&a,&b,&c);

    (a+b+c==180 && a!=0 && b!=0 && c!=0) ? printf("It's a triangle!\n") : printf("The triangle is not valid!\n");

   
}